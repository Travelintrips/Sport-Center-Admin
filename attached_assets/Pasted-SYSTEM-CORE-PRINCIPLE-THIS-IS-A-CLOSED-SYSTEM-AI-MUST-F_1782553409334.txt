SYSTEM CORE PRINCIPLE
THIS IS A CLOSED SYSTEM.

AI MUST FOLLOW THIS SPEC EXACTLY.
AI IS NOT ALLOWED TO DEVIATE, IMPROVISE, OR SUBSTITUTE ARCHITECTURE.
🚨 SYSTEM RULES (IMMUTABLE — ABSOLUTE HIGHEST PRIORITY)
DATABASE LOCK (NON-NEGOTIABLE)
ONLY VALID DATABASE:
- Supabase PostgreSQL

ABSOLUTE FORBIDDEN:
- ANY OTHER DATABASE SYSTEM (NO EXCEPTIONS)
  - Includes: Firebase, MongoDB, SQLite, MySQL external, HeliumDB, etc.

RULE ENFORCEMENT:
- If not Supabase → IT IS INVALID AND MUST NOT BE USED
- AI MUST NEVER suggest or reference alternative databases
🧨 ZERO-DEVIATION RULE
AI MUST NOT:
- Modify architecture rules
- Replace database system
- Invent new infrastructure
- Assume missing systems
- Use external tools not defined in this spec

IF INFORMATION IS MISSING:
→ AI MUST ASK USER
→ NEVER GUESS
🧠 DEFAULT BEHAVIOR LOCK
- If unsure about storage → ALWAYS USE SUPABASE
- Never create alternative persistence layer
- Never redesign architecture
🛑 FAILURE HANDLING PROTOCOL
IF ANY CONFLICT WITH RULES OCCURS:

STEP 1 → STOP IMMEDIATELY
STEP 2 → DO NOT GENERATE CODE
STEP 3 → ASK USER FOR CLARIFICATION
STEP 4 → WAIT FOR INSTRUCTION
🔧 CODE ENFORCEMENT (STRICT MODE)
- ALL data persistence MUST use Supabase client only
- NO other DB SDKs allowed
- NO fallback database implementations
- NO mock replacement of DB in production logic
🔍 MANDATORY PRE-CODE VALIDATION
BEFORE writing ANY code involving data:

AI MUST VERIFY:

1. Is database = Supabase?
2. Is schema inside `sport_center` namespace?
3. Is no external DB referenced?

IF ANY ANSWER IS NO:
→ ABORT IMMEDIATELY
→ REWRITE OR ASK USER
🎯 PRIORITY HIERARCHY (ABSOLUTE ORDER)
1. SYSTEM RULES (ZERO-DEVIATION CORE)
2. DATABASE LOCK (Supabase only)
3. ARCHITECTURE SPEC
4. DATA INTEGRITY
5. USER REQUESTS
6. PERFORMANCE OPTIMIZATION
7. CODE STYLE
🧱 ANTI-HALLUCINATION RULE
AI MUST NOT:
- Assume missing APIs
- Assume missing tables
- Assume external services
- Assume unknown business logic

IF NOT SPECIFIED:
→ ASK USER
→ DO NOT INVENT
🏟️ SPORT CENTER JAKARTA — PROJECT SPEC

Web application untuk manajemen dan pemesanan fasilitas olahraga:

Customer booking portal
Admin management portal
⚙️ STACK
pnpm workspaces
Node.js 24
TypeScript 5.9
Frontend
React + Vite
Wouter
TanStack Query
shadcn/ui
Tailwind CSS
Recharts
Backend
Express 5 REST API
Database
Supabase PostgreSQL (ONLY)
ORM
Drizzle ORM
Validation
Zod
📁 ARCHITECTURE
OpenAPI spec → Orval → React Query hooks (contract-first)
Frontend ONLY uses generated API hooks
Backend serves REST API
Database ONLY Supabase schema sport_center
NO public schema usage
🔐 AUTH SYSTEM

JWT stored in:

localStorage.sport_center_token

Password hashing:

HMAC-SHA256(password, SESSION_SECRET)
💳 PAYMENT SYSTEM
Manual bank transfer only
Upload proof URL
Admin verification required
No payment gateway integrations
🏟️ BUSINESS LOGIC
Availability slots generated from facility open/close hours
Validation against:
existing bookings
blocked schedules
🧑‍💻 MODULES
Customer Portal
Home
Facilities
Booking
Invoice
Promo
Contact
Admin Portal
Dashboard
Bookings
Facilities CRUD
Schedule blocking
Customers
Promos
Settings
⚠️ SYSTEM GOTCHAS
- Always run codegen after OpenAPI change
- Never edit generated API client
- DB is shared Supabase instance (sport_center schema only)
- Anonymous bookings ≠ registered customers
📌 REFERENCES ONLY
OpenAPI: lib/api-spec/openapi.yaml
DB schema: lib/db/src/schema/
Generated API: lib/api-client-react/src/generated/api.ts