PRODUCTION TRANSACTION INTEGRITY REMEDIATION
SPORT CENTER — CONTINUE AUTOMATICALLY
FAIL-CLOSED FOR AMBIGUOUS FINANCIAL DATA

PRODUCTION GATES SUDAH PASS:

Production Runtime              = PASS
Production Database Identity    = PASS
Read-only Database              = PASS

Production data audit menemukan:

sport_bookings              = 429
sport_payments              = 374
company_invoices            = 4
booking_groups              = 47
accounting_journals         = 380
accounting_journal_lines    = 211
tax_transactions            = 1,134
bank_reconciliation_matches = 68
payment_accounting_outbox   = 362

ANOMALIES:

1. 403 completed bookings
   - 376 tanpa checked_in_at
   - 8 tanpa completed_at
   - 2 completed tetapi end time masih future

2. Payment:
   - 16 duplicate groups berdasarkan booking_id + payment_type
   - 32 payment rows
   - 70 confirmed/completed bookings tanpa confirmed payment

3. Payment accounting outbox:
   - 323 posted
   - 27 processing
   - 12 failed

4. Reconciliation:
   - 68 candidate
   - candidate_source belum tersedia pada:
     sport_center.bank_reconciliation_matches

5. Cross-entity integrity:
   - booking/payment anomalies
   - reconciliation anomalies

NO DATA WAS MUTATED DURING AUDIT.

==================================================
GLOBAL SAFETY CONTRACT
==================================================

JANGAN:

- menghapus payment
- menghapus journal
- menghapus journal line
- menghapus settlement
- menghapus reconciliation
- menghapus booking
- menghapus invoice
- menghapus historical data
- melakukan blind UPDATE
- melakukan blind DELETE
- melakukan blind INSERT accounting
- backfill accounting
- menjalankan migration reconciliation secara otomatis jika berisiko collision
- mengubah Central Finance mode
- mengubah finance architecture
- membuat duplicate journal
- mengubah settlement
- mengubah payment amount
- mengubah tax amount
- mengubah historical financial meaning

Central Finance harus tetap untouched kecuali repair benar-benar terbukti
idempotent, canonical, dan aman.

Untuk setiap remediation:

AUDIT → CLASSIFY → PROVE → FIX ONLY SAFE CASES → VERIFY

Jika evidence tidak cukup:

KEEP DATA
MARK REVIEW
DO NOT MUTATE

==================================================
PHASE 1 — FREEZE BASELINE
==================================================

Ambil baseline sebelum perubahan:

- booking count
- payment count
- invoice count
- journal count
- journal line count
- tax transaction count
- reconciliation count
- outbox count

Simpan checksum/identifier baseline.

Git status harus dicatat.

==================================================
PHASE 2 — BOOKING COMPLETION ANOMALIES
==================================================

Audit SEMUA booking status=completed.

Untuk setiap booking:

- booking id
- status
- check-in record
- checked_in_at
- completed_at
- start datetime
- end datetime
- booking history
- lifecycle history
- WhatsApp completion evidence jika ada
- scheduler evidence jika ada
- admin status history jika ada
- reschedule history

==================================================
RULE A — FUTURE COMPLETED
==================================================

Cari completed booking dengan:

endTime > NOW()

atau equivalent timezone-safe comparison.

Ini INVALID.

Namun:

JANGAN langsung ubah status.

Cari history untuk menentukan apakah completion terjadi karena:

- old bug
- admin action
- scheduler
- WhatsApp
- invoice
- payment

Jika completion jelas salah dan booking belum dimainkan:

boleh diperbaiki HANYA jika target state dapat ditentukan
secara deterministik dari lifecycle history.

Expected:

confirmed / waiting_confirmation / cancelled / rescheduled

Jangan menebak.

Jika ambigu:

REVIEW_REQUIRED.

==================================================
RULE B — COMPLETED WITHOUT completed_at
==================================================

Untuk 8 rows:

Cari completion timestamp dari:

- booking history
- lifecycle history
- audit log
- scheduler event
- WhatsApp completion event

Jika hanya ada satu timestamp yang jelas dan authoritative:

boleh backfill completed_at.

Jika lebih dari satu atau tidak jelas:

JANGAN update.

Mark REVIEW_REQUIRED.

==================================================
RULE C — COMPLETED WITHOUT CHECK-IN
==================================================

Untuk 376 rows:

JANGAN mengisi checked_in_at berdasarkan:

- booking date
- start time
- payment time
- invoice date
- completed_at

Cari evidence check-in authoritative:

- check-in table
- QR check-in record
- check-in history
- lifecycle event

Jika evidence check-in ditemukan secara unik:

boleh memperbaiki checked_in_at menggunakan timestamp
authoritative tersebut.

Jika tidak ada evidence:

JANGAN membuat checked_in_at.

Jangan memalsukan histori.

Classification:

VERIFIED_CHECKIN
atau
NO_CHECKIN_EVIDENCE

Untuk NO_CHECKIN_EVIDENCE:

jangan mengubah data sampai akar masalah historical completion
ditentukan.

==================================================
PHASE 3 — PAYMENT DUPLICATES
==================================================

16 duplicate groups:

GROUP BY:

booking_id + payment_type

Audit setiap row:

- payment id
- booking id
- payment type
- amount
- status
- payment date
- provider reference
- external transaction id
- correlation id
- created_at
- updated_at
- accounting linkage
- settlement linkage

JANGAN DELETE DUPLICATES.

==================================================
CANONICAL PAYMENT RULE
==================================================

Tentukan canonical payment hanya jika:

1. same booking
2. same payment_type
3. same logical payment attempt
4. provider reference identifies same transaction
5. exactly one row is authoritative

Jika duplicate is exact technical duplicate AND
canonical row is unambiguous:

safe remediation may be limited to:

- mark duplicate as invalid/duplicate ONLY IF existing schema supports it
- preserve original row
- preserve audit history
- do not delete

Jika schema tidak memiliki safe duplicate status:

DO NOT MUTATE.

==================================================
PAYMENT WITHOUT CONFIRMED PAYMENT
==================================================

70 confirmed/completed bookings without confirmed payment.

Jangan otomatis membuat payment.

Classify:

A. Corporate billing booking
B. Payment exempt / invoice billing
C. Payment pending
D. Payment missing
E. Historical legacy anomaly

Corporate bookings may legitimately have no direct confirmed payment.

Personal booking without payment:

REVIEW_REQUIRED unless historical evidence proves payment elsewhere.

Never create fake payment rows.

==================================================
PHASE 4 — OUTBOX
==================================================

27 processing
12 failed

Audit each:

- payment_id
- event type
- attempts
- created_at
- updated_at
- last error
- accounting journal linkage
- correlation id
- existing public accounting entry
- existing journal
- existing journal lines

==================================================
SAFE OUTBOX RULE
==================================================

Before retry:

CHECK canonical accounting entry.

If accounting already exists:

DO NOT post again.

Mark/reconcile outbox only if existing workflow
supports idempotent recovery.

If no accounting entry exists:

verify payment is valid and canonical.

Only then retry using EXISTING accounting worker/service.

DO NOT directly INSERT journal rows.

DO NOT manually create journal entries.

DO NOT bypass Central Finance.

For stale processing rows:

determine whether worker is actually running.

Only mark/requeue if existing application state machine
explicitly supports it.

Otherwise REVIEW_REQUIRED.

==================================================
PHASE 5 — RECONCILIATION
==================================================

68 candidate rows.

DO NOT automatically approve.

Audit each candidate:

- candidate_type
- candidate_id
- bank mutation
- payment
- settlement
- amount
- date
- identity
- source
- existing match
- duplicate match
- status

Known schema conflict:

sport_center.bank_reconciliation_matches
does NOT contain candidate_source.

public.bank_reconciliation_matches
does contain candidate_source.

DO NOT blindly migrate or copy rows.

DO NOT swap canonical source.

DO NOT approve candidate.

DO NOT alter settlement.

==================================================
CANDIDATE_SOURCE REMEDIATION
==================================================

First determine which table is CURRENT CANONICAL runtime table.

Search:

- API routes
- reconciliation service
- SQL queries
- approval endpoint
- settlement service
- schema
- migrations
- tests

If runtime uses:

sport_center.bank_reconciliation_matches

and candidate_source is required for source identity:

do NOT immediately migrate Production.

Prepare:

- schema conflict report
- migration plan
- collision analysis
- backward compatibility analysis

Migration may only be executed if:

- no collision
- all existing rows can be mapped
- application compatibility is proven
- Central Finance is unaffected

Otherwise:

BLOCKED_REMEDIATION

No production schema mutation.

==================================================
PHASE 6 — CROSS-ENTITY BOOKING/PAYMENT
==================================================

For every booking:

booking
→ payment
→ invoice
→ accounting
→ settlement
→ reconciliation

Classify:

VALID
MISSING
DUPLICATE
MISMATCH
NOT_APPLICABLE
REVIEW_REQUIRED

Do not manufacture missing relationships.

==================================================
PHASE 7 — CENTRAL FINANCE SAFETY
==================================================

Recalculate:

journal debit total
journal credit total

Verify:

debit = credit

Then verify:

payment_id uniqueness
correlation ID uniqueness
duplicate accounting entry
journal ↔ payment
outbox ↔ journal

DO NOT modify journals.

DO NOT backfill journals.

DO NOT change finance mode.

DO NOT touch historical Central Finance data.

If an outbox repair requires posting:

use existing idempotent worker ONLY.

==================================================
PHASE 8 — TAX
==================================================

Existing audit:

1,134 tax transactions
PPN_OUT_11
rate 11.00

Verify against actual Sport Center tax contract.

Do not rewrite historical tax transactions.

If historical discrepancy is found:

REVIEW_ONLY.

==================================================
PHASE 9 — EVENT
==================================================

Run the missing event audit.

Verify:

bookingType = event

discount:

214 / 1000

Expected:

Rp100,000
→ discount Rp21,400
→ final Rp78,600

Audit:

- regular event
- recurring event
- duration
- facility price
- discount
- final amount
- invoice/payment linkage

Do not modify historical event amounts automatically.

==================================================
PHASE 10 — RECURRING
==================================================

Run full audit:

- past dates
- today elapsed slots
- facility operating hours
- closing time
- duration
- duplicate dates
- conflicts
- blocked schedules
- corporate approval
- waiting_confirmation
- confirmed
- completed

Compare every recurring rule against single-booking contract.

Do not mutate existing historical bookings unless correction
is deterministic and explicitly safe.

==================================================
PHASE 11 — RESCHEDULE
==================================================

Audit:

- request
- original booking
- new schedule
- approval
- history
- status
- conflict
- check-in
- completion

Verify atomicity in code.

No pending requests currently exist, but audit historical requests.

==================================================
PHASE 12 — REFUND
==================================================

No refund table was found.

Do NOT create one.

Search code/schema for:

refund
refunded
payment reversal
credit note

If refund functionality exists elsewhere:

audit the actual source.

==================================================
PHASE 13 — EXPENSE
==================================================

sport_expenses = 0

Do not create data.

Only verify code path and accounting integration.

==================================================
PHASE 14 — AUTOMATIC FIX POLICY
==================================================

SAFE TO FIX AUTOMATICALLY only when:

- evidence is deterministic
- no financial meaning is changed
- no duplicate accounting risk
- no settlement risk
- no reconciliation ambiguity
- existing lifecycle/service method can perform fix
- operation is idempotent
- transaction is atomic
- audit history is preserved

Examples potentially safe:

- correcting invalid future completed status when authoritative lifecycle
  history proves target status
- restoring missing completed_at from one authoritative event
- restoring checked_in_at from unique authoritative check-in record
- safely recovering stale outbox using existing idempotent worker

NOT SAFE TO FIX AUTOMATICALLY:

- deleting duplicate payments
- changing payment amounts
- deleting reconciliation
- approving candidates
- changing settlement
- changing journal
- creating missing payments
- creating accounting entries manually
- changing historical tax
- guessing check-in timestamps
- guessing booking status
- schema migration with unresolved source collision

For unsafe cases:

DO NOT MUTATE.
Produce exact remediation list.

==================================================
PHASE 15 — VERIFICATION AFTER SAFE FIXES
==================================================

After every safe mutation:

- rerun affected audit
- verify row count
- verify lifecycle consistency
- verify payment uniqueness
- verify accounting balance
- verify reconciliation integrity
- verify no duplicate journal
- verify no Central Finance mutation outside intended idempotent recovery

Then run:

- full tests
- typecheck
- build
- git diff --check

==================================================
PHASE 16 — FINAL REPORT
==================================================

Report:

SAFE FIXES APPLIED
REVIEW REQUIRED
BLOCKED REMEDIATIONS
NO-TOUCH FINANCIAL ITEMS

For every mutation provide:

table
record id
old state
new state
reason
evidence
method used
timestamp

For every blocked item provide:

record/group
problem
why ambiguous
required owner decision or follow-up

Final mandatory statements:

PRODUCTION DATA MUTATION = ONLY SAFE VERIFIED FIXES
CENTRAL FINANCE = SAFE / NO UNAUTHORIZED MUTATION
ACCOUNTING BALANCE = PASS/ANOMALY
PAYMENT DUPLICATE = RESOLVED/REVIEW
BOOKING LIFECYCLE = RESOLVED/REVIEW
RECONCILIATION = RESOLVED/BLOCKED
OUTBOX = RESOLVED/REVIEW
EVENT = VERIFIED
RECURRING = VERIFIED
RESCHEDULE = VERIFIED
CROSS-ENTITY = VERIFIED/REVIEW

Do not stop at audit-only if a safe deterministic fix exists.

Do not ask for confirmation.

Continue automatically until:
1. all safe fixes are completed,
2. all unsafe/ambiguous items are isolated,
3. tests/build pass,
4. final verification is complete.

Central Finance must remain protected throughout.