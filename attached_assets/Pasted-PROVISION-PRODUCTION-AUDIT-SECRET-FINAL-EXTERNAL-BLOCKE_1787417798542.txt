PROVISION PRODUCTION AUDIT SECRET — FINAL EXTERNAL BLOCKER
SPORT CENTER — CONTINUE WITHOUT CONFIRMATION

CURRENT STATUS:

Audit runner                         PASS
Audit command                        PASS
Scripts typecheck                    PASS
API typecheck                        PASS
git diff --check                     PASS
Production mutation                  NONE
Central Finance                      UNCHANGED

PostgreSQL audit role:

sport_center_production_auditor      PASS
CONNECT                              PASS
sport_center USAGE                   PASS
public USAGE                         PASS
reconciliation SELECT                PASS
default_transaction_read_only        ON

CURRENT BLOCKER:

SUPABASE_PROD_AUDIT_DATABASE_URL
is not available through the official Production Secret Manager loader.

==================================================
OBJECTIVE
==================================================

Resolve the missing Production audit secret.

Required logical secret:

SUPABASE_PROD_AUDIT_DATABASE_URL

Source of truth:

Google Secret Manager

Use the existing official bootstrap:

GCP_SECRET_MANAGER_BOOTSTRAP_JSON

Do NOT create a new authentication mechanism.

==================================================
PHASE 1 — DETERMINE EXACT GCP TARGET
==================================================

Use the existing official Secret Manager loader/configuration.

Determine the actual:

- GCP project ID
- Secret Manager project
- secret naming convention
- bootstrap principal

Do NOT guess the project from:

sc-sport-center
sport-center-506306
sc-sport-center-506314

Use only the values resolved by the official existing Production bootstrap/configuration.

Do not print credential values.

==================================================
PHASE 2 — CHECK SECRET EXISTENCE
==================================================

Check Secret Manager metadata for:

SUPABASE_PROD_AUDIT_DATABASE_URL

Do NOT read/print the secret payload unless absolutely required by the
official loader to perform the connection test.

Sanitized output only:

SECRET EXISTS = YES
or
SECRET EXISTS = NO

==================================================
PHASE 3 — IF SECRET EXISTS
==================================================

Verify the official loader can access:

SUPABASE_PROD_AUDIT_DATABASE_URL

Do not expose the value.

Then run:

pnpm --filter @workspace/scripts run audit:production-read-only

The runner must verify:

current_user =
sport_center_production_auditor

transaction_read_only =
on

Do NOT run the full historical audit yet.

Only perform the safety handshake.

==================================================
PHASE 4 — IF SECRET DOES NOT EXIST
==================================================

Determine whether the authenticated bootstrap principal has permission to
create the secret and add a version.

Required secret:

SUPABASE_PROD_AUDIT_DATABASE_URL

IMPORTANT:

The secret value must be the PostgreSQL connection string for the existing
Production role:

sport_center_production_auditor

It must NOT be:

SUPABASE_DATABASE_URL
SUPABASE_DATABASE_URL_DEV
DATABASE_URL
SUPABASE_SERVICE_ROLE_KEY

Do not fabricate the connection string.

Do not generate a new PostgreSQL password.

The connection string must come from the already-created Production
PostgreSQL audit role.

==================================================
PHASE 5 — IF AUTHORIZED TO CREATE SECRET
==================================================

If the bootstrap principal has explicit Secret Manager permissions:

Create only:

SUPABASE_PROD_AUDIT_DATABASE_URL

Do not modify existing secrets.

Do not delete existing secrets.

Do not rotate application credentials.

Do not change:

SUPABASE_DATABASE_URL

Do not change:

GCP_SECRET_MANAGER_BOOTSTRAP_JSON

Do not change application secrets.

Add only the required secret/version.

Then verify metadata/access without printing payload.

==================================================
PHASE 6 — IF SECRET EXISTS BUT ACCESS IS DENIED
==================================================

Do NOT bypass IAM.

Do NOT use another credential.

Do NOT copy the secret into Replit Secrets.

Report:

SECRET EXISTS = PASS
SECRET ACCESS = BLOCKED

Identify the exact missing permission if available.

==================================================
PHASE 7 — IF SECRET DOES NOT EXIST AND CREATE IS DENIED
==================================================

Stop at the external IAM boundary.

Report exactly:

EXTERNAL BLOCKER:
AUTHORIZED SECRET MANAGER PRINCIPAL REQUIRED

Required secret:

SUPABASE_PROD_AUDIT_DATABASE_URL

Do not claim the audit secret exists.

Do not claim Production read-only access.

==================================================
PHASE 8 — SAFETY HANDSHAKE
==================================================

Once secret access succeeds, run only the initial connection handshake.

The runner must execute:

BEGIN;
SET TRANSACTION READ ONLY;
SHOW transaction_read_only;

Expected:

on

Then:

SELECT current_user;
SELECT current_database();

Expected user:

sport_center_production_auditor

Then:

ROLLBACK;

Do not execute the historical audit yet.

==================================================
PHASE 9 — FINAL VERIFICATION
==================================================

Run:

pnpm --filter @workspace/scripts run typecheck

pnpm --filter @workspace/api-server run typecheck

pnpm --filter @workspace/scripts run audit:production-read-only

git diff --check

No Production mutation.

No payment modification.

No booking modification.

No accounting modification.

No settlement modification.

No reconciliation modification.

No Central Finance modification.

==================================================
FINAL VERDICT
==================================================

If secret is successfully provisioned and handshake passes:

AUDIT SECRET = PASS
AUDIT SECRET ACCESS = PASS
AUDIT ROLE = PASS
PRODUCTION DATABASE = PASS
READ-ONLY = PASS
PRODUCTION RECORD-LEVEL AUDIT = READY

If IAM blocks provisioning:

AUDIT SECRET = BLOCKED
AUDIT SECRET ACCESS = BLOCKED
READ-ONLY DB = BLOCKED
PRODUCTION AUDIT = BLOCKED
EXTERNAL IAM ACTION REQUIRED

Do not ask for confirmation.
Continue automatically until the external permission boundary.