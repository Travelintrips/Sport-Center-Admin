CONNECT SUPABASE PRODUCTION AUDIT SECRET — CONTINUE AUTOMATICALLY

Current result:

audit command = PASS
runner = PASS / fail-closed
typecheck = PASS
git diff --check = PASS

Current blocker:

PRODUCTION_AUDIT_FAIL_CLOSED:
SUPABASE_PROD_AUDIT_DATABASE_URL is required.

The PostgreSQL audit role has already been created in Supabase Production:

sport_center_production_auditor

Database privileges verified:

CONNECT = true
sport_center schema USAGE = true
public schema USAGE = true
reconciliation SELECT = true
default_transaction_read_only = on

DO NOT modify the PostgreSQL role again.

DO NOT use:

SUPABASE_DATABASE_URL
SUPABASE_DATABASE_URL_DEV
DATABASE_URL
SUPABASE_SERVICE_ROLE_KEY

==================================================
OBJECTIVE
==================================================

Connect the dedicated Production audit secret to the existing official
Google Secret Manager loader.

Required logical secret:

SUPABASE_PROD_AUDIT_DATABASE_URL

Source of truth:

Google Secret Manager

Existing bootstrap:

GCP_SECRET_MANAGER_BOOTSTRAP_JSON

==================================================
PHASE 1 — INSPECT LOADER
==================================================

Inspect the existing Production Secret Manager loader and determine exactly
how application secrets are loaded.

Reuse the existing loader.

Do NOT create another GCP authentication mechanism.

Determine:

- GCP project source
- secret naming convention
- bootstrap parsing
- Secret Manager access method
- environment mapping

==================================================
PHASE 2 — RESOLVE AUDIT SECRET
==================================================

Make the Production audit runner able to resolve:

SUPABASE_PROD_AUDIT_DATABASE_URL

through the existing official Secret Manager mechanism.

Do NOT add a fallback to:

SUPABASE_DATABASE_URL

Do NOT add a fallback to:

SUPABASE_DATABASE_URL_DEV

Do NOT copy the application database credential.

==================================================
PHASE 3 — SECRET EXISTENCE
==================================================

Check whether the Secret Manager secret exists.

Do NOT print its value.

Allowed output:

SUPABASE_PROD_AUDIT_DATABASE_URL = AVAILABLE

or:

SUPABASE_PROD_AUDIT_DATABASE_URL = NOT_FOUND

If NOT_FOUND:

STOP at the external provisioning boundary.

Do not fabricate a value.

Do not create a PostgreSQL credential.

Do not create a secret unless the authenticated GCP principal is explicitly
authorized to create it.

==================================================
PHASE 4 — ENVIRONMENT ISOLATION
==================================================

The audit secret may ONLY be exposed to:

NODE_ENV=production-audit

It must NOT be exposed to:

- normal API
- frontend
- mockup
- DEV
- tests
- browser/client code

==================================================
PHASE 5 — RUNNER
==================================================

After the loader can resolve the secret, run:

pnpm --filter @workspace/scripts run audit:production-read-only

The runner must prove:

1. audit URL available
2. current_user =
   sport_center_production_auditor
3. current_database = Production
4. transaction_read_only = on

If any gate fails:

STOP.

Do not use fallback credentials.

==================================================
PHASE 6 — SECURITY
==================================================

Never print:

- password
- connection string
- secret payload
- hostname if it would expose credentials

Only print sanitized verification status.

==================================================
PHASE 7 — DO NOT RUN FULL AUDIT YET
==================================================

Even if the connection succeeds, first perform only the safety handshake:

BEGIN;
SET TRANSACTION READ ONLY;
SHOW transaction_read_only;

Verify:

on

Verify:

SELECT current_user;
SELECT current_database();

Then ROLLBACK.

Do NOT start the full historical audit until this handshake is PASS.

==================================================
PHASE 8 — VALIDATION
==================================================

Run:

pnpm --filter @workspace/scripts run typecheck

pnpm --filter @workspace/scripts run audit:production-read-only

git diff --check

Do not mutate Production.

Do not retry outbox.

Do not approve reconciliation.

Do not modify payment.

Do not modify booking.

Do not modify accounting.

Do not modify Central Finance.

==================================================
FINAL VERDICT
==================================================

If secret does not exist:

AUDIT SECRET = BLOCKED
READ-ONLY DB = BLOCKED
PRODUCTION AUDIT = BLOCKED
EXTERNAL ACTION REQUIRED = CREATE/PROVISION SECRET

If secret exists but runner cannot access it:

AUDIT SECRET = EXISTS
AUDIT SECRET ACCESS = BLOCKED

If handshake succeeds:

AUDIT SECRET = PASS
AUDIT ROLE = PASS
PRODUCTION DATABASE = PASS
READ-ONLY TRANSACTION = PASS
PRODUCTION RECORD-LEVEL AUDIT = READY

Do not ask for confirmation.
Continue automatically until the external provisioning boundary.